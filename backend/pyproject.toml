[project]
name = "latuapasta-ordering-backend"
version = "0.1.0"
description = "Backend for the La Tua Pasta batch ordering planner (Centric + ERP + Power BI)"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "requests>=2.31",
    "python-dotenv>=1.0",
]
